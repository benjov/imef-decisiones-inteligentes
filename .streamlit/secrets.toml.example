# Copia este archivo como .streamlit/secrets.toml para correr en local.
# En Streamlit Community Cloud, pega este contenido en Settings → Secrets.
# NUNCA subas secrets.toml al repo (está en .gitignore).

ANTHROPIC_API_KEY = "sk-ant-..."
